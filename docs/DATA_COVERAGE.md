# Data coverage contract

- `verified`: all displayed admission facts trace to official documents.
- `partial`: institution identity is known, but at least one admission layer is missing.
- `unverified`: never used in score recommendations.

The UI must never imply nationwide program coverage until ingestion coverage reports prove it.
