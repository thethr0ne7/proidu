# ПРОЙДУ? minimized GitHub build

Static build for GitHub Pages. Uses live Supabase Edge Functions from env-injected constants inside `app.js`.

Claim policy: do not show “1000+ verified” until coverage endpoint reports real counts passing gate.
